# ⚠️ 生产构建配置：以下两项在 build 时内联进产物，改完必须重新 `pnpm build`
# 发布前必须填真实值（详见 docs/DEPLOYMENT.md）——漏配会导致
# sitemap/robots/RSS/canonical/OG 全部输出 localhost

# 后端地址：/api 与 /uploads 反向代理目标
# BACKEND_URL=http://api:3001

# 站点公开地址：sitemap/robots/RSS/OpenGraph/canonical 使用
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
